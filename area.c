#include<stdio.h>//stdio.h = header file
int main(){//main = funtcion,which returns integer type value
  int side;//int = Datatype,side = variable
printf("enter side: ");
scanf("%d",&side);//%d = format specifier
printf("Area is %d",side*side);
return 0;
}
output
enter side: 4
  Area is 16
